const express = require("express");
const { addedContact } = require("../controllers/contactController");


// const router = express.Router();

// router.post("/", addedContact);

// module.exports = router;
